# Generated by IBM TransformationAdvisor
# Mon Oct 29 16:16:35 UTC 2018

apiVersion: v1
kind: Service
metadata:
  name: jpetstoreapp
  labels:
    app: jpetstoreapp
spec:
  ports:
    - port: 9080
  selector:
    app: jpetstoreapp
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: jpetstoreapp
  name: jpetstoreapp
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: jpetstoreapp
    spec:
      containers:
      - image: mycluster.icp:8500/default/jpetstoreapp:latest
        name: jpetstoreapp
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
